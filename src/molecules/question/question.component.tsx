import { Typography } from '@mui/material';

export default function Question() {
  return (
    <Typography
      variant="subtitle1"
      component="p"
    >
      question component
    </Typography>
  );
}
